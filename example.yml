---
#
# The common role performs the common configuration and decrufting steps for a base SmartOS zone
#
#- hosts: common
#  roles:
#  - common
#  vars:
#    ansible_python_interpreter: # allows the user to specify a python intrepreter other than the SmartOS default of /opt/local/bin/python
#
#
